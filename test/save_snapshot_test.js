
var expect = require('chai').expect;

describe('convertImageTo', () => {
    const saveImage = require('../controllers/snapshot_controller').saveImage;

    it('it should save the image to file storage', (done) => {

    });
});
